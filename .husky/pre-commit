staged_files=$(git diff --cached --name-only --diff-filter=ACM -- '*.md' | grep -E '^(apps|dev)/' || true)
if [ -n "$staged_files" ]; then
  echo "$staged_files" | xargs node scripts/validate-markdown.mjs
fi
